apiVersion: v1
kind: Service

metadata:
  name: campaigns-api

  labels:
    app: campaigns-api

  annotations:
    description: "Service para a API de Campanhas, expondo a porta 80 para acesso interno no cluster."

spec:
  type: NodePort

  selector:
    app: campaigns-api
    
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30081

# acessar localmente: http://localhost:30081/